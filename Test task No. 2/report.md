# Развёртывание окружения

## Окружение

- Операционная система: Kali Linux
- Node.js: v20.20.2
- npm: 10.8.2

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/versions.JPG)

---

## Процесс развёртывания

### Вариант 1 — Docker

Первая попытка развёртывания была выполнена с использованием официального Docker-образа, рекомендованного в документации проекта.

```bash
docker run --rm -p 3000:3000 bkimminich/juice-shop:v17.1.1
```

Развёртывание не удалось, так как Docker не смог загрузить образ из Docker Hub.

```text
Unable to find image 'bkimminich/juice-shop:v17.1.1' locally
docker: Error response from daemon:
Get "https://registry-1.docker.io/v2/":
dial tcp: lookup registry-1.docker.io:
i/o timeout
```

---

### Вариант 2 — Сборка из исходного кода

Вторая попытка была выполнена с использованием официального репозитория GitHub.

```bash
git clone https://github.com/juice-shop/juice-shop.git
cd juice-shop
git checkout v17.1.1
npm install
```

Во время установки зависимостей npm сообщил о конфликте зависимостей.

```text
npm ERR! code ERESOLVE
npm ERR! ERESOLVE could not resolve
...
```

Для решения проблемы было предпринято несколько попыток, включая использование разных версий Node.js и установку зависимостей с параметром `--legacy-peer-deps`. Однако устранить конфликт зависимостей в локальном окружении не удалось.

---

### Вариант 3 — Готовая официальная сборка

Чтобы продолжить выполнение задания, была использована официальная готовая сборка (prebuilt release), опубликованная разработчиками OWASP Juice Shop.

```bash
wget https://github.com/juice-shop/juice-shop/releases/download/v17.1.1/juice-shop-17.1.1_node20_linux_x64.tgz
```

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/clone%20of%20the%20repository.JPG)

```bash
tar -xzvf juice-shop-17.1.1_node20_linux_x64.tgz
cd juice-shop_17.1.1_node20_linux_x64
npm start
```

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/launching%20npm.JPG)

После этого приложение успешно запустилось.

```text
info: Detected Node.js version v20.20.2 (OK)
info: Port 3000 is available (OK)
```

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/OWASP%20Juice%20Shop.JPG)

---

## Возникшие сложности

Перед успешным запуском приложения были предприняты две попытки развёртывания.

Первый вариант с использованием Docker не удалось реализовать, поскольку Docker не смог загрузить образ из Docker Hub из-за проблем с сетевым подключением.

Вторая попытка, предусматривавшая сборку приложения из исходного кода, завершилась конфликтом зависимостей при установке пакетов. Для решения проблемы были предприняты несколько попыток, включая смену версии Node.js и использование альтернативных параметров установки npm, однако устранить конфликт не удалось.

Чтобы продолжить выполнение задания, была использована официальная готовая сборка OWASP Juice Shop. Это позволило успешно запустить приложение, сохранив весь необходимый функционал для проведения анализа безопасности.

## Часть 2. Анализ уязвимостей OWASP Juice Shop

### Окружение

- Приложение: OWASP Juice Shop v17.1.1
- URL: http://localhost:3000
- Браузер: Google Chrome
- Операционная система: Kali Linux
- Метод анализа: ручное тестирование с использованием инструментов разработчика браузера (DevTools)

Во время анализа автоматические сканеры уязвимостей (Burp Suite, OWASP ZAP, Nikto) не использовались.

### Краткое резюме

| Приоритет | Уязвимость | Риск |
|-----------|------------|------|
| **КРИТИЧЕСКИЙ** | SQL Injection | Обход аутентификации, полный доступ к базе данных |
| **ВЫСОКИЙ** | Brute Force | Автоматический перебор паролей, компрометация аккаунтов |
| **ВЫСОКИЙ** | Vulnerable and Outdated Components | Обход аутентификации через уязвимый пакет express-jwt |
| **СРЕДНИЙ** | Sensitive Data Exposure | Утечка конфиденциальной информации |
| **СРЕДНИЙ** | Security Misconfiguration | Отсутствие защитных заголовков безопасности |

**Наиболее критичная уязвимость:** SQL Injection, позволяющая обойти механизм входа в систему и получить права администратора.

---

# 1.Отсутствие защиты от брутфорса (Brute Force)

### OWASP Top 10

**A07:2021 – Identification and Authentication Failures**

### Где обнаружена

```
POST /rest/user/login
```

### Как была обнаружена

С помощью небольшого баш-скрипта был проведён автоматизированный перебор паролей для учётной записи `admin@juice-sh.op`. За 100 попыток аккаунт не был заблокирован, rate limiting не сработал.

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/bruteforce.JPG)

В результате учетная запись администратора была полностью скомпрометирована
![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/adminOK.JPG)

Примечание: пароль не скрываю, т.к это учебный уязвимый стенд

### Чем грозит бизнесу

- Автоматический подбор паролей без ограничений
- Компрометация аккаунтов пользователей
- Взлом административного доступа

### Рекомендации по устранению

- Внедрить rate limiting — не более 5 попыток входа с одного IP за 15 минут
- Добавить CAPTCHA после 3 неудачных попыток
- Настроить логирование всех неудачных попыток входа
- Настроить алертинг при аномальном количестве попыток
- Использовать блокировку аккаунта на временной интервал

---

# 2. Sensitive Data Exposure

### OWASP Top 10

**A02:2021 – Cryptographic Failures (Раскрытие конфиденциальной информации)**

### Где обнаружена

```
http://localhost:3000/ftp
```

### Как была обнаружена

Во время ручного исследования приложения был обнаружен открытый FTP-каталог.

В каталоге находятся внутренние файлы приложения, например:

- encrypt.py
- quarantine
- legal
- coupons_2013.md
- incident-support.kdbx

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part2/Sensitive%20Data%20Exposure%20(ftp)3.JPG)

### Чем грозит бизнесу

Публичный доступ к внутренним файлам может привести к раскрытию:

- конфиденциальной информации компании;
- резервных копий;
- внутренней документации;
- файлов с учётными данными или базами паролей.

Эта информация может использоваться злоумышленниками для подготовки дальнейших атак на инфраструктуру компании.

### Рекомендации по устранению

- Запретить публичный доступ к внутренним каталогам.
- Хранить конфиденциальные файлы вне директории веб-приложения.
- Использовать механизмы аутентификации и авторизации.
- Регулярно проверять ресурсы, доступные без авторизации.

---

# 3. Security Misconfiguration

### OWASP Top 10

**A05:2021 – Неправильная конфигурация безопасности (Security Misconfiguration)**

### Где обнаружена

HTTP Response Headers

### Как была обнаружена

С помощью вкладки **Network** в инструментах разработчика браузера были проанализированы HTTP-заголовки ответа.

Было обнаружено отсутствие нескольких рекомендуемых заголовков безопасности:

- Content-Security-Policy
- Strict-Transport-Security
- Referrer-Policy

Кроме того, использовался устаревший заголовок **Feature-Policy** вместо современного **Permissions-Policy**.

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part2/Security%20Misconfiguration2.JPG)

### Чем грозит бизнесу

Отсутствие защитных HTTP-заголовков увеличивает риск:

- Cross-Site Scripting (XSS);
- атак типа Man-in-the-Middle;
- утечки информации через HTTP Referer;
- злоупотребления возможностями браузера.

### Рекомендации по устранению

Настроить следующие защитные HTTP-заголовки:

- Content-Security-Policy
- Strict-Transport-Security
- Referrer-Policy
- Permissions-Policy

Также рекомендуется проверить конфигурацию веб-сервера в соответствии с рекомендациями проекта **OWASP Secure Headers**.

---
# 4. Vulnerable and Outdated Components

### OWASP Top 10

**A06:2021 – Vulnerable and Outdated Components**

### Где была обнаружена

```
package.json
```
### Как была обнаружена

В ходе анализа зависимостей приложения был обнаружен критически устаревший пакет `express-jwt` версии **0.1.3**.

Актуальная версия на данный момент - **8.0.0**. Разница в версиях составляет более **7 релизов**, что указывает на наличие множества неисправленных уязвимостей.
В версиях express-jwt до 5.3.3 существует уязвимость, связанная с недостаточной проверкой JWT-токенов. Злоумышленник может использовать поддельный токен для обхода механизма аутентификации и получить несанкционированный доступ к защищённым маршрутам приложения.

**Доказательство:**
```
"express-jwt": "0.1.3"
```
![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/A06.JPG)

### Чем грозит бизнесу

- Обход аутентификации
- Несанкционированный доступ к защищённым маршрутам
- Получение прав администратора без авторизации
- Утечка данных пользователей
- Это может привести к компрометации пользовательских аккаунтов и успешным фишинговым атакам.

### Рекомендации по устранению
- Немедленно обновить пакет:
- Запустить проверку всех зависимостей:
- Внедрить регулярный аудит зависимостей в CI/CD
- Настроить Dependabot для автоматического обновления пакетов
---

# 5. SQL Injection

### OWASP Top 10

**A03:2021 – Injection (SQL Injection)**

### Где обнаружена

Страница авторизации.

### Как была обнаружена

Во время ручного тестирования формы входа была выявлена возможность SQL-инъекции.

Приложение приняло SQL Injection payload при попытке аутентификации.

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part2/sql%20inj5.png)

После этого приложение автоматически выполнило вход под учётной записью администратора.

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part2/payloadSQL.png)

### Чем грозит бизнесу

SQL Injection позволяет злоумышленнику:

- обходить механизм аутентификации;
- читать конфиденциальные данные из базы данных;
- изменять существующие записи;
- удалять информацию;
- получить полный контроль над базой данных.

Данная уязвимость является критической, поскольку нарушает конфиденциальность, целостность и доступность данных.

### Рекомендации по устранению

- Использовать параметризованные SQL-запросы (Prepared Statements).
- Никогда не объединять пользовательский ввод со строкой SQL-запроса.
- Выполнять проверку и валидацию пользовательского ввода.
- Использовать принцип минимально необходимых привилегий для учётных записей базы данных.

---

## Итог

В ходе анализа были выявлены пять различных классов уязвимостей:

| Уязвимость | Категория OWASP |
|------------|-----------------|
| Brute Force (отсутствие rate limiting) | A07:2021 |
| Sensitive Data Exposure | A02:2021  |
| Security Misconfiguration | A05:2021  |
| Vulnerable and Outdated Components | A06:2021  |
| SQL Injection | A03:2021  |

Все уязвимости были обнаружены вручную с использованием браузера и инструментов разработчика без выполнения атак на внешние системы.


# Часть 3. Анализ TLS-конфигурации

### Сайт 1 - Xsolla (https://xsolla.com)

### 1. Версии TLS

Проверка выполнена с помощью OpenSSL:

```bash
openssl s_client -connect xsolla.com:443 -servername xsolla.com -brief
```
![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3/OpenSSL%20xs.JPG)

Результат:

- Используется **TLS 1.3**
- Устаревшие версии TLS 1.0 и TLS 1.1 не используются.
- Во время проверки сервер согласовал соединение по TLS 1.3.

---

### 2. Набор шифров

Во время TLS-рукопожатия был выбран шифр:

```
TLS_AES_256_GCM_SHA384
```

Особенности:

- современный алгоритм;
- поддерживает AEAD-шифрование;
- обеспечивает конфиденциальность и целостность данных;
- слабые шифры (RC4, 3DES, DES, NULL и т.п.) не используются.

По результатам проверки признаков использования небезопасных алгоритмов не обнаружено.

---

### 3. Сертификат

Проверка выполнялась командами:

```bash
echo | openssl s_client -connect xsolla.com:443 \
-servername xsolla.com \
-showcerts
```

и

```bash
echo | openssl s_client -connect xsolla.com:443 \
-servername xsolla.com 2>/dev/null |
openssl x509 -noout -issuer -subject -dates
```
![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3/certificates%20xs.JPG)

## Полученная информация:

**Сертификат сайта**
Subject:
CN = xsolla.com

**Издатель**
Cloudflare TLS Issuing ECC CA 3

**Срок действия**
Not Before: 09.06.2026
Not After: 25.12.2026

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3/Validity%20period.JPG)

Во время проверки была получена полная цепочка сертификатов:

```
xsolla.com
↓
Cloudflare TLS Issuing ECC CA 3
↓
SSL.com TLS Transit ECC CA R2
↓
SSL.com TLS ECC Root CA 2022
↓
AAA Certificate Services
```


OpenSSL выполнил успешную проверку сертификата:

```
Verification: OK
```

Это означает, что сертификат действителен и цепочка доверия построена корректно.

---

### 4. HSTS

Проверка выполнялась:

```bash
curl -I https://xsolla.com
```

Получены заголовки:

```
Strict-Transport-Security:
max-age=31536000;
includeSubDomains;
preload
```
![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3/HSTS%20xs.JPG)

Следовательно:

- HSTS включён;
- max-age = 31536000 секунд (1 год);
- используется includeSubDomains;
- используется preload.

Такая конфигурация соответствует современным рекомендациям OWASP.

---

### 5. Проверка через SSL Labs

Дополнительно была выполнена проверка через:

```https://www.ssllabs.com/ssltest/```

Полученный результат:

Overall Rating: A-

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3/SSL%20Labs%20xs.JPG)

Оценка показывает высокий уровень TLS-конфигурации сервера.

## Вывод

TLS-конфигурация сайта Xsolla выполнена на высоком уровне.

Проверка показала:

- используется современный протокол TLS 1.3;
- отсутствуют слабые алгоритмы шифрования;
- сертификат действителен и имеет корректную цепочку доверия;
- HSTS настроен корректно с длительным временем действия, поддержкой поддоменов и preload.

Критичных недостатков, связанных с TLS, обнаружено не было.




### Сайт 2 - Paypal (https://www.paypal.com)

### 1. Версии TLS
Проверка выполнена с помощью OpenSSL:

```
openssl s_client -connect paypal.com:443 -servername paypal.com -brief 
```

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3!/sssl%20pp.JPG)

### Результат:

- Используется **TLS 1.3**
- Устаревшие версии TLS 1.0 и TLS 1.1 отключены
- Соединение устанавливается успешно

Версия TLS 1.2 не показана в выводе -brief, так как клиент и сервер автоматически выбрали самую новую. 
Для проверки поддержки 1.2 пришлось бы использовать -tls1_2 (но это не обязательно, так как мы уже видели, что современный протокол работает).

### 2. Набор шифров
Во время TLS-рукопожатия был выбран шифр:

```
TLS_AES_128_GCM_SHA256
```
Особенности:

- используется современный алгоритм  AES-128;
- режим GCM обеспечивает одновременно шифрование и контроль целостности;
- слабые алгоритмы (RC4, 3DES, NULL, EXPORT) отсутствуют.

### Интересная деталь:

В выводе указано Negotiated TLS1.3 group: X25519MLKEM768. Это означает, что PayPal (через Fastly) использует постквантовое шифрование (гибридный обмен ключами X25519 + ML-KEM).

Это очень современная и продвинутая настройка безопасности, что подтверждает высокий уровень их инфраструктуры

### 3. Проверка цепочки сертификатов

```
echo | openssl s_client -connect paypal.com:443 -servername paypal.com -showcerts 2>/dev/null | grep -E "s:|i:|v:|Verify return code"
```

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3!/cert%20pp.JPG)

Получено:
- сертификат выдан доверенным удостоверяющим центром;
- цепочка сертификатов полная;
- срок действия сертификата актуален (с 18 января 2024 г. по 9 ноября 2031 г);
- проверка сертификата завершается успешно (`Verify return code: 0 (ok)`).

Обычно серверы отдают только Leaf и Intermediate, а Root браузер берет из своего хранилища. Тот факт, что PayPal  отдает все три, говорит об их максимальной совместимости со старыми клиентами

### 4. HSTS
Проверка:

```
curl -I https://www.paypal.com
```

Основной домен paypal.com отвечает кодом 301 Moved Permanently и перенаправляет пользователя на https://www.paypal.com/

Конечный домен www.paypal.com возвращает корректный заголовок HSTS:
```
strict-transport-security: max-age=63072000; includeSubDomains; preload
```

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3!/HSTSpp.JPG)

Вывод по HSTS:
- HSTS включён;
- время действия политики — 2 года;
- используется `includeSubDomains`;
- используется `preload`.
- Все рекомендации OWASP соблюдены.

### 5.Проверка через SSL Labs
SSL Labs присвоил оценку A-, что также считается высоким уровнем безопасности 

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3!/sslLabs%20pp.JPG)


## Вывод
Конфигурация демонстрирует максимальный уровень корпоративной безопасности.
Используется TLS 1.3, постквантовое шифрование (X25519MLKEM768), полная цепочка из 3 сертификатов (включая корневой), и самый строгий HSTS из возможных

### Сайт 3 - PHTT (https://phtt.ru)

### 1. Версии TLS
Проверка выполнена с помощью OpenSSL:

```
openssl s_client -connect phtt.ru:443 -servername phtt.ru -brief 2>&1 | head -n 10
```

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3-phtt/ssl%20sh.JPG)

Дополнительно была выполнена проверка TLS 1.0

```
openssl s_client -connect phtt.ru:443 -servername phtt.ru -tls1 -brief 2>&1 | head -n 5
```

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3-phtt/main%20test%20sh.JPG)

Анализ:

Сервер поддерживает самый современный протокол TLS 1.3. Ошибка alert protocol version при попытке подключиться по TLS 1.0 подтверждает, что устаревшие версии полностью отключены. 

Это хороший показатель безопасности для небольшого образовательного учреждения

### 2. Набор шифров

Во время TLS-рукопожатия был выбран шифр:

```
TLS_AES_256_GCM_SHA384
```

Особенности:
- Современный алгоритм (AES-256 в режиме GCM).
- Обеспечивает конфиденциальность и целостность (AEAD).
- Слабые шифры (RC4, 3DES, NULL) отсутствуют.


### 3. Цепочка сертификатов
Проверка выполнялась командой:

```
echo | openssl s_client -connect phtt.ru:443 -servername phtt.ru -showcerts 2>/dev/null | grep -E "s:|i:|v:|Verify return code"
```
![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3-phtt/certs%20sh.JPG)

Получено:
- сертификат выдан Let's Encrypt;
- цепочка сертификатов полная;
- срок действия сертификата актуален: с 28.07.2026 по 26.10.2026;
- проверка завершилась успешно (`Verify return code: 0 (ok)`).

### 4. HSTS

Проверка выполнялась командой 
```
curl -I https://phtt.ru:
```
![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3-phtt/HSTS%20sh.JPG)

Анализ:

max-age: 31536000 секунд (1 год) - это соответствует минимальным рекомендациям безопасности.
includeSubDomains: ОТСУТСТВУЕТ
preload: ОТСУТСТВУЕТ

### 5. Проверка через SSL Labs

**Полученный результат:**
Overall Rating: **A+**

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/part3-phtt/ssl%20labs%20sh.JPG)

**Анализ:**
SSL Labs дал максимальную оценку A+ благодаря использованию TLS 1.3, сильным шифрам (AES-256-GCM) и полной цепочке сертификатов. 
Отсутствие `includeSubDomains` и `preload` в HSTS не повлияло на итоговый балл автоматизированного сканера.

---

## Возможный ущерб
Если в инфраструктуре существуют поддомены (например, `mail.phtt.ru`, `student.phtt.ru`), которые пользователь открывает впервые, браузер не будет принудительно использовать HTTPS.

При нахождении злоумышленника в одной сети с пользователем возможна атака типа **Man-in-the-Middle**. В этом случае злоумышленник сможет перенаправить пользователя на незашифрованное HTTP-соединение и попытаться перехватить передаваемые данные.

Следует отметить, что данный риск является **скорее потенциальным**, поскольку современные браузеры по умолчанию предпочитают HTTPS, а эксплуатация требует выполнения нескольких дополнительных условий. Тем не менее добавление директив `includeSubDomains` и `preload` полностью устраняет данный сценарий.

Данный недостаток легко устраняется изменением конфигурации веб-сервера

### Мой личный итог по уровню защищенности

Если оценивать сайты, то я бы расставила их так:

**1. PayPal — Супер защита**
PayPal демонстрирует абсолютный максимум того, что возможно в 2026 году. Это не просто «хорошо настроенный» сайт, это индустриальный стандарт. Помимо обязательного TLS 1.3 и сильных шифров, они используют гибридный алгоритм обмена ключами X25519MLKEM768, передают полную цепочку из 3 сертификатов и имеют максимально жесткий HSTS на 2 года с `preload`. Здесь нет ни одной зацепки для атакующего.

**2. Xsolla — Очень хорошо**
Xsolla (за счет Cloudflare) обеспечивает уровень безопасности, который полностью закрывает все базовые и современные требования (TLS 1.3, надежные шифры, HSTS с preload). Оценка SSL Labs A- вместо A+ - это не проблема самого сайта, а скорее особенности конфигурации CDN. Тем не менее, это по-прежнему очень высокая планка.

**3. phtt.ru — Формально отлично, фактически есть нюанс**
Этот сайт стал для меня самым интересным. С точки зрения чистой криптографии он получил высший балл **A+**, и его TLS 1.3 с AES-256 защищен не хуже, чем у PayPal. Однако, когда я заглянула в HTTP-заголовки, я увидела, что политика HSTS настроена лишь наполовину (нет `includeSubDomains` и `preload`). 

**Мой вывод:** На практике это означает, что если у техникума есть какой-то незащищенный поддомен (например, старый форум студентов), злоумышленник в локальной сети все еще может попытаться перехватить трафик. Это **не критическая дыра**, но она показывает, что автоматические сканеры не всегда способны на полноценный анализ системы




# Часть 4. Скрипт проверки security-заголовков

## Подход

Скрипт (`check_headers.sh`) принимает список доменов - либо файлом (`domains.txt`, один домен на строку, `#` для комментариев), либо аргументами командной строки.

Для каждого домена делает `curl -I` (HEAD-запрос) с ограничением на время соединения и на весь запрос, проверяет пять заголовков и пишет результат построчно в
`security_headers_report.csv`, и также дает цветной вывод в терминал

## Почему «заголовок есть» - недостаточный критерий

Присутствие заголовка не означает, что он реально защищает.
Например, `Strict-Transport-Security: max-age=1` формально существует, но действует одну секунду - никакой практической пользы. 
Поэтому статус GOOD скрипт ставит не по факту наличия заголовка, а по конкретному порогу для каждого:

| Заголовок | GOOD | WARN | Обоснование порога |
|---|---|---|---|
| HSTS | `max-age ≥ 15552000` (180 дней) | заголовок есть, но `max-age` меньше или не указан | Минимум по рекомендации OWASP и Mozilla — короткий `max-age` даёт защиту только на короткое окно, после истечения браузер снова доверяет первому подключению по HTTP |
| CSP | политика задана, без явно опасных директив | содержит `unsafe-inline`, `unsafe-eval` или wildcard `*` в `script-src`/`default-src` | Эти директивы фактически отключают главный смысл CSP — ограничение источников исполняемого кода |
| X-Frame-Options | `DENY` или `SAMEORIGIN` | любое другое значение (например устаревший `ALLOW-FROM`) | Только эти два значения поддерживаются всеми современными браузерами и реально блокируют clickjacking |
| X-Content-Type-Options | `nosniff` | любое другое значение | Это единственное осмысленное значение заголовка — он либо включён, либо нет |
| Referrer-Policy | любая политика, кроме `unsafe-url` | `unsafe-url` | `unsafe-url` передаёт полный referrer (включая query-параметры) даже при переходе на сторонний и незашифрованный сайт |

Если заголовок отсутствует полностью — статус `MISSING`. 
Итоговый статус домена (`overall`) равен худшему из пяти: если хотя бы один `MISSING` - домен помечается `MISSING` целиком, даже если остальные четыре `GOOD`.

## Обработка ошибок

Недоступный домен, таймаут и битый сертификат не должны останавливать проверку остальных доменов из списка.
Это реализовано так: `curl` вызывается без флага `-k`, то есть проверка сертификата **включена**

Если сертификат недействителен (просрочен, самоподписан) или домен не отвечает - `curl` возвращает пустой результат, и такой домен получает
статус `ERROR` по всем колонкам, а скрипт переходит к следующему домену в списке, не прерывая прогон.

Изначально в скрипте был флаг `-k`, отключающий проверку сертификата.

От него отказались: с `-k` домен с битым сертификатом выглядел бы в отчёте так же, как полностью исправный - заголовки бы проверились и показали `GOOD`, при том что сам факт нерабочего TLS остался бы незамеченным. 
Для инструмента, который называется «проверка безопасности», это неприемлемо: скрытая проблема хуже отсутствия проверки.

## Список доменов

`domains.txt` включает:
- `xsolla.com`, `paypal.com` - компании из условия задания;
- `google.com`, `yandex.ru`, `vk.com`, `gosuslugi.ru` — крупные
  сервисы для сравнения;
- `phtt.ru` — сайт моего техникума, добавлен для личного интереса и
  использован также в Части 3 для TLS-разбора;
- `fdxf.com` — заведомо несуществующий домен, добавлен намеренно,
  чтобы проверить ветку `ERROR` и убедиться, что она не роняет прогон.

## Результаты прогона

| Домен | HSTS | CSP | XFO | XCTO | RP | Overall |
|---|---|---|---|---|---|---|
| xsolla.com | GOOD | MISSING | GOOD | MISSING | MISSING | MISSING |
| paypal.com | GOOD | WARN | MISSING | GOOD | MISSING | MISSING |
| fdxf.com | ERROR | ERROR | ERROR | ERROR | ERROR | ERROR |
| phtt.ru | GOOD | MISSING | MISSING | MISSING | MISSING | MISSING |
| google.com | MISSING | MISSING | GOOD | MISSING | MISSING | MISSING |
| yandex.ru | MISSING | WARN | MISSING | GOOD | MISSING | MISSING |
| vk.com | MISSING | MISSING | MISSING | MISSING | MISSING | MISSING |
| gosuslugi.ru | GOOD | MISSING | MISSING | MISSING | MISSING | MISSING |

![](https://github.com/Pwnboberry/xsolla2/blob/main/screenshots/check_headers.JPG)

`fdxf.com` корректно получил `ERROR` по всем колонкам, не сломав проверку остальных семи доменов - обработка ошибок работает как задумано.

## Наблюдение: интуиция vs реальность

Ожидалось, что крупные компании (Google, PayPal, Xsolla) покажут `GOOD` почти везде. По факту ни один домен в списке не получил `Overall: GOOD` - у всех отсутствует хотя бы один заголовок. 
Проверила это независимо через securityheaders.com: `google.com` там получает оценку **C**, при том что их же поддомен `apis.google.com` - оценку **A**. 

Это подтверждает, что результат не баг скрипта, а реальная практика: заголовки настраиваются не на уровне компании, а на уровне конкретного сервиса. 
Старые, тяжело поддерживаемые страницы (главные лендинги) часто отстают по security-гигиене от новых изолированных сервисов той же компании - вероятно, из-за риска сломать сторонние интеграции при добавлении строгого CSP.

## Связь с Частью 3

TLS (Часть 3) и HTTP security-заголовки (Часть 4) - независимые слои защиты. У `xsolla.com` отличная TLS-конфигурация (TLS 1.3, современный шифр, корректная цепочка сертификатов, правильный HSTS), но при этом
отсутствуют CSP, X-Content-Type-Options и Referrer-Policy. 
Хороший транспортный уровень не компенсирует пробелы на уровне приложения - это два разных вопроса, и сайт может быть отличным в одном и слабым в другом одновременно


